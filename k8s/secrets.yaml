apiVersion: v1
kind: Secret
metadata:
  name: beta-code-secrets
  namespace: beta-code
type: Opaque
# Values are base64-encoded. To update:
#   echo -n 'yourvalue' | base64
stringData:
  POSTGRES_PASSWORD: "postgres"
  # Set a strong secret to authenticate the runner against the frontend API
  RUNNER_SECRET: ""
