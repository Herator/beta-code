apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: beta-code

resources:
  - namespace.yaml
  - secrets.yaml
  - configmap.yaml
  - postgres.yaml
  - redis.yaml
  - frontend.yaml
  - runner.yaml
  - ingress.yaml
  - nfs-storageclass.yaml
  # Apply keda-scaledobject.yaml only after KEDA is installed:
  #   kubectl apply -f keda-scaledobject.yaml
  # - keda-scaledobject.yaml
